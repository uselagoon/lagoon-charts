{{- if and .Values.betaUI.enabled .Values.betaUI.ingress.enabled -}}
{{- $fullName := include "lagoon-core.betaUI.fullname" . -}}
{{- $svcPort := .Values.betaUI.service.port -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "lagoon-core.betaUI.labels" . | nindent 4 }}
  annotations:
    {{- with coalesce .Values.betaUI.ingress.ingressClassName .Values.defaultIngressClassName }}
    acme.cert-manager.io/http01-ingress-class: {{ . }}
    {{- end }}
    {{- with .Values.betaUI.ingress.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- with coalesce .Values.betaUI.ingress.ingressClassName .Values.defaultIngressClassName }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.betaUI.ingress.tls }}
  tls:
    {{- range .Values.betaUI.ingress.tls }}
  - hosts:
      {{- range .hosts }}
      - {{ . | quote }}
      {{- end }}
    secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
  {{- range .Values.betaUI.ingress.hosts }}
  - host: {{ .host | quote }}
    http:
      paths:
      {{- range .paths }}
      - path: {{ . }}
        pathType: Prefix
        backend:
          service:
            name: {{ $fullName }}
            port:
              number: {{ $svcPort }}
      {{- end }}
  {{- end }}
{{- end }}
